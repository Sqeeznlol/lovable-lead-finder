// Score V1 ersetzt durch V3 (HNF-basiert).
// Re-Export haelt alle bestehenden Imports gueltig:
//   import { calculateDealScore, scoreColor, scoreBg } from '@/lib/deal-score';
export * from './deal-score-v2';
